Changes in version 0.3:

This release compresses a 15% faster but produces files a 1% larger.

Option "--test" now takes precedence over option "--stdout".

When finished decompressing, "done" is shown instead of "ok".
